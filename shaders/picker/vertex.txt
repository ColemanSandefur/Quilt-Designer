#version 140

in vec2 position;
in vec4 color;
in mat4 model;
in uint id;

flat out uint v_id;

uniform mat4 view;
uniform mat4 projection;

void main() {
    v_id = id;
    gl_Position = projection * view * model * vec4(position, 1.0, 1.0);
}