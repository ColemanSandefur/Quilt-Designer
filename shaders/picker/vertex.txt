#version 140

in vec2 position;
in uint id;

flat out uint v_id;

uniform vec4 color;

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

void main() {
    v_id = id;
    gl_Position = projection * view * model * vec4(position, 1.0);
}