#version 140

in vec2 position;
out vec4 input_color;

uniform vec4 color;

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

void main() {
    input_color = color;
    gl_Position = projection * view * model * vec4(position, 0.95, 1.0);
}