#version 140

in vec2 position;
in vec4 color;
in mat4 model;

out vec4 input_color;

uniform mat4 view;
uniform mat4 projection;

void main() {
    input_color = color;
    gl_Position = projection * view * model * vec4(position, 1.0, 1.0);
}